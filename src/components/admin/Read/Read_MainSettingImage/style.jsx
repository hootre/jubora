import styled from '@emotion/styled';
import { Common } from 'styles/Common';

export const Read_MainSettingImage_container = styled.section`
  .mainImage {
    transform: translateX(-18%);
  }
  .logo {
    transition: none;
  }
  > span {
    margin-bottom: 10px;
  }

  .notice_box {
    width: 100%;
    display: flex;
    .notice {
      transform: none;
      width: 800px;
    }
  }
`;
