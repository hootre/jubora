import styled from '@emotion/styled';
import { Common } from 'styles/Common';

export const CardList_container = styled.div`
  padding: 50px 0;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 50px;
`;
